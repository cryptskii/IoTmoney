{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug executable 'IoT.money'",
            "program": "${workspaceFolder}/target/debug/IoT.money",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
